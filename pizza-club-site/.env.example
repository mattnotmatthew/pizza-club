# Copy this file to .env and fill in your values

# Google Maps API Configuration
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
VITE_GOOGLE_MAPS_ID=your_map_id_here

# Supabase Authentication Configuration
# Get these from your Supabase project: Settings > API
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Photo Upload Configuration (Optional)
# If these are set, photos will be uploaded to your server instead of stored as base64
# Leave empty to use local base64 storage
# Note: You can use any directory name that doesn't conflict with existing apps
VITE_UPLOAD_API_URL=https://yourdomain.com/pizza-upload/upload.php
VITE_UPLOAD_API_TOKEN=your_secret_upload_token_here

# Database API Configuration (Optional)
# If this is set, the app will use the database API instead of JSON files
# Leave empty to use JSON files only
# Note: Uses the same token as VITE_UPLOAD_API_TOKEN for authentication
VITE_API_URL=https://yourdomain.com/pizza_api